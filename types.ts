export type Breakpoints = {
  sm: number;
  md: number;
  lg: number;
  xl: number;
  xxl: number;
}

export type Colours = {
  primary: string;
  secondary: string;
}

export type Layers = {
  base: number;
  over: number;
  top: number;
  under: number;
}

export enum Orientation {
  Landscape = 'landscape',
  Portrait = 'portrait'
}
